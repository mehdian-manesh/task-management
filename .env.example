HTTP_PORT=80
HTTPS_PORT=443
# DOMAIN: hostname to serve (e.g. localhost or tm.local). For custom domains like tm.local,
# add "127.0.0.1 tm.local" to your hosts file so the browser can resolve it.
DOMAIN=localhost
DJANGO_SECRET_KEY=local-dev-secret
DB_PASSWORD=local-db-password
